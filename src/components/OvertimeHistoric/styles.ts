
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    ResumeTitle: {
        marginLeft: '2.5%',
        marginTop: 18,
        fontSize: 16,
        fontWeight: 'bold'
    },
    List: {
        height: 460
    }
});

export default styles;
